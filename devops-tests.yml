# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pool:
  name: 'TecConnect50Pool'

steps:
- script: echo Starting Devops Tests!
  displayName: 'Starting Devops Tests'

- script: echo Build.BuildId = $(Build.BuildId); Build.BuildNumber = $(Build.BuildNumber); Prueba $(BuildNumber)
  displayName: 'Test predefined variables'

- script: echo Starting Devops Tests!
  displayName: 'Starting Devops Tests'
- task: DotNetCoreCLI@2
  displayName: 'Build'
  condition: eq(variables.Build,'true')
  inputs:
    command: 'build'
    projects: 'TodoApi.csproj'
